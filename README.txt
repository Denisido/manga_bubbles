📁 Структура проекта manga_news
manga_news/
├── prompts/
│   ├── prompt1.js        # Запрос для генерации ироничной новости
│   ├── prompt2.js        # Запрос для генерации сценария/кадров с пузырями
│   └── prompt3.js        # (опционально) Запрос для генерации описания сцены/фона
├── data/
│   ├── news.txt          # Сгенерированная ироничная новость (plain text)
│   ├── scenario.json     # Главный сценарий — массив кадров (frames), с пузырями внутри каждого кадра
│   ├── manga_base.png    # Одна сгенерированная картинка — общее полотно для комикса
│   └── output.png        # Финальный результат — полотно с отрисованными пузырями
├── scripts/
│   ├── generate_news.js      # Скрипт: генерирует новость (использует prompt1)
│   ├── scenario_gen.js       # Скрипт: генерирует scenario.json (использует prompt2)
│   └── render_bubbles.js     # Рисует все пузыри по scenario.json на manga_base.png
│   └── export.js             # Запускает headless-браузер, делает скриншот результата (для Telegram и т.п.)
├── index.html                # Визуализация через Konva + JS, запускается в Puppeteer
├── package.json
└── README.md

💡 Содержимое и роль основных файлов:


prompts/prompt1.js
Содержит текст prompt'а для генерации ироничной новости.

prompts/prompt2.js
Содержит prompt для генерации сценария: описания кадров и массива пузырей внутри каждого кадра.
Сюда вносишь любые новые инструкции, если хочешь изменить структуру scenario.json.
Можно расширять — например, для передачи дополнительных параметров сцен.

data/news.txt
Сохраняет сгенерированную новость.
Используется как входной текст для следующего этапа (scenario_gen.js).

data/scenario.json
Главный JSON-файл, который содержит массив кадров:
"scenario": параметры кадра (frame, x, y, width, height, описание)
"speechBubble": массив пузырей или null
Все координаты пузырей локальные внутри кадра!
Используется на этапе рендера.

data/manga_base.png
Одна общая картинка, под которую будет подложен весь комикс.
Генерируется отдельно (AI, DALL-E, Stable Diffusion, вручную).

data/output.png
Итоговый файл — картинка с уже “наложенными” пузырями, готовая к публикации.

scripts/generate_news.js
Запускает prompt1 → получает новость → сохраняет в news.txt.

scripts/scenario_gen.js
Берёт новость из news.txt, запускает prompt2 → получает и сохраняет scenario.json.

scripts/render_bubbles.js
Загружает scenario.json.
Отрисовывает все пузыри на общем полотне (с учётом глобальных координат).
Можно расширять: стилизация, спецэффекты, отладочные маркеры.

scripts/export.js
Запускает headless Puppeteer, открывает index.html, ждёт рендера, делает скриншот контейнера с пузырями и сохраняет файл (например, output.png).

index.html
Минимальная страница: подгружает Konva, JS-скрипт (render_bubbles.js), создаёт div#container для рисования.
Менять не требуется — только если хочешь добавить предпросмотр, кнопки для тестов или сменить размеры полотна.

package.json / README.md
Содержит зависимости (Puppeteer, fs, Konva), инструкции по запуску.